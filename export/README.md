# export

Go command-line tool (`ocm`) that connects to an authorization server and exports its OIDC/OAuth2
client configurations into the [canonical client configuration format](../docs/canonical-client-config-v0.1.md).

Supports Keycloak and PingFederate as source systems.

See [docs/exporting.md](../docs/exporting.md) for build and usage instructions.
